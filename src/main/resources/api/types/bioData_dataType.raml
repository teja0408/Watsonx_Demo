#%RAML 1.0 DataType
type: object
properties:
  _id:
    description: A default field for BSON ObjectId's and it is, by default, indexed. _id and id  are not the same
    properties:
      $oid: string
  id: string
  name: string
  fullname: string
  birthplace: string
  base: string
  publisher: string
  occupation: string